// Next.js 應用程式設定檔
// 用 JSDoc @type 提供型別提示，不需要 TypeScript 也能有自動完成功能

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // 開啟嚴格模式：幫助提早發現過時 API 或潛在問題
}

module.exports = nextConfig
